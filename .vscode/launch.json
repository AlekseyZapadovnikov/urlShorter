{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug URL‑Shortener",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/url-shorter",
            "cwd": "${workspaceFolder}",
            "env": {
                "CONFIG_PATH": "${workspaceFolder}/config/config.json",
                "DB_PASSWORD": "123"
            }
        }
    ]
}